import React from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Footer = (props) => (
  <div className={"react-component section__content section__content--" + props.theme}>
  </div>
);

export default Footer;