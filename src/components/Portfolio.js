import React from 'react';

const Portfolio = (props) => (
  <section className="portfolio">
    <h2 className={"portfolio__title section__title section__title--" + props.theme}>Portfolio</h2>
  </section>
);

export default Portfolio;